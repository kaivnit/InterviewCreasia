import React from 'react';

const EmployeeAttendance: React.FC = () => {
    return (
        <div>
            <h1>Employee Attendance</h1>
            {/* Nội dung của component */}
        </div>
    );
};

export default EmployeeAttendance;

// Thêm câu lệnh export trống để biến file này thành một module
export {};